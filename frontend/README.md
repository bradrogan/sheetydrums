# sheetydrums frontend

Vite + TypeScript + VexFlow. Loads `events.json` and renders an interactive drum score with synced audio playback.

## TODO (skeleton phase)

- [ ] Scaffold Vite + TS project
- [ ] Type definitions generated from `../schema/events.schema.json`
- [ ] Render a single hardcoded bar with VexFlow (proves the rendering pipeline)
- [ ] Load `events.json` and render all bars
- [ ] Audio playback synced to a playhead
- [ ] Click-to-seek on the score
